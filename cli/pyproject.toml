[project]
name = "codemate-cli"
version = "0.1.0"
description = "CodeMate Docker launcher"
requires-python = ">=3.9"
dependencies = [
  "click>=8.1",
]

[project.scripts]
codemate = "codemate_cli.main:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
