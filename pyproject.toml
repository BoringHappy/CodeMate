[project]
name = "codemate-cli"
version = "0.1.0"
description = "CodeMate Docker launcher"
requires-python = ">=3.9"
dependencies = [
  "rich>=13.0",
  "typer>=0.12",
]

[project.scripts]
codemate = "codemate_cli.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
